# torch/torchvision +cu126 wheels live on the PyTorch index, not PyPI.
--extra-index-url https://download.pytorch.org/whl/cu126

# Core ML / inference
torch==2.10.0+cu126
torchvision==0.25.0+cu126
triton==3.6.0
numpy==2.4.4
opencv-python-headless==4.13.0.92
pillow==12.1.1
scipy==1.17.1

# ONNX (FaceID + ViTPose ONNX runtimes)
onnx==1.21.0
onnxruntime-gpu==1.25.0
protobuf==7.34.1
ml_dtypes==0.5.4
flatbuffers==25.12.19

# CUDA wheels (pulled in by torch, pinned for reproducibility — CUDA 12.6)
nvidia-cublas-cu12==12.6.4.1
nvidia-cuda-cupti-cu12==12.6.80
nvidia-cuda-nvrtc-cu12==12.6.77
nvidia-cuda-runtime-cu12==12.6.77
nvidia-cudnn-cu12==9.10.2.21
nvidia-cufft-cu12==11.3.0.4
nvidia-cufile-cu12==1.11.1.6
nvidia-curand-cu12==10.3.7.77
nvidia-cusolver-cu12==11.7.1.2
nvidia-cusparse-cu12==12.5.4.2
nvidia-cusparselt-cu12==0.7.1
nvidia-nccl-cu12==2.27.5
nvidia-nvjitlink-cu12==12.6.85
nvidia-nvshmem-cu12==3.4.5
nvidia-nvtx-cu12==12.6.77

# Segmentation — SAM 3 (editable install from external/sam3)
-e external/sam3
timm==1.0.27
ftfy==6.1.1
iopath==0.1.10
portalocker==3.2.0
tqdm==4.67.3

# Transformers / HF (CLIP attribute captioner in anonymization/prompt_utils)
transformers==5.6.2
huggingface_hub==1.14.0
tokenizers==0.22.2
safetensors==0.7.0
regex==2026.5.9

# Video I/O
av==17.0.1

# Config / pipeline
hydra-core==1.3.2
omegaconf==2.3.0
PyYAML==6.0.3

# Process management (ComfyUI server discovery + low-RAM restart guard)
psutil==7.2.2

# Mask RLE utils (track cache)
pycocotools==2.0.11

# Re-ID (OSNet via torchreid)
torchreid==0.2.5
gdown==5.2.0

# Face identification (KPL face embeddings — buffalo_l weights cached at ~/.insightface/models/).
# matplotlib is a hard insightface dependency.
insightface==0.7.3
matplotlib==3.10.9

# RTMW/DWPose renderer (optional; only loaded when diffusion.yaml pose_renderer: dwpose).
# rtmlib auto-fetches weights from the MMPose CDN on first use into ~/.cache/rtmlib.
rtmlib==0.0.15

# Op-edit web UI
fastapi==0.136.1
uvicorn[standard]==0.47.0

# Misc (torch / omegaconf transitive, pinned for reproducibility)
filelock==3.25.2
fsspec==2026.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.6.1
packaging==26.2
sympy==1.14.0
typing_extensions==4.15.0
antlr4-python3-runtime==4.9.3
setuptools==70.2.0
